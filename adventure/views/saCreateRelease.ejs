<%- include("head", {title: "Add a release for " + product.Name}) %>
<h1>Add a release for <%= product.Name %></h1>
Fill in a name for the release, and the slug for the URL. The other fields can be filled in when the release is created.
<form method="post" action="/sa/createRelease/<%= product.ProductUUID %>">
    <div class="row">
        <div class="form-group col">
            <label for="name">Name</label>
            <input required type="text" class="form-control" id="name" name="name" placeholder="Name">
        </div>
        <div class="form-group col">
            <label for="slug">Slug</label>
            <input required type="text" class="form-control" id="slug" name="slug" placeholder="Slug" pattern="^[0-9A-Za-z-]+$">
            <small class="form-text text-muted">
                Alphanumeric characters and hyphen only. This must be unique.
            </small>
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
    <a class="btn btn-outline-primary" href="/product/<%= product.Slug %>">Cancel</a>
    <a class="btn btn-outline-primary" href="/sa/product/<%= product.ProductUUID %>">Edit</a>
</form>
<%- include("foot") %>